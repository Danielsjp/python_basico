"""
CARACTERÍSTICAS BÁSICAS DE PYTHON
"""

# VARIABLES
# Una variable es un espacio en memoria para guardar datos
# por tanto, es un contenedor
# Para crear una variable...
# identificador = valor
# Hay reglas para llamar a los identificadores = nombre de la variable

# No se puede hacer:
# 1var|able, empezar por numero, ni empezar ni contener simbolos.
# De estos errores nos avisará VSC

# No debemos usar caracteres fuera del idioma ingles, ñ, ç, etc...
# tampoco debemos empezar por guión bajo.

# ---------------------------------

# Que debemos hacer
# Nomb

#git: git config --global user.name daniel.sanchez
#   git config --global user.email sjp.daniel@gmail.com
# git config core autocrlf true
# si no estas en windows: git config --global core.autocrlf true 
# git config --global core.editor "code --wait"
# editar ver fichero: git config --global -e   
# git init
# git status 
# git add 0*
# git add .  ( esto lo sube todo menos lo del ignore)
# .gitignore
# git rm --cached 04_strings.py (esto lo quita de A y lo retrocede a U)

# la A hace que git este pendiente de cada cambio
# en la U todavia no monitoriza ningun cambio